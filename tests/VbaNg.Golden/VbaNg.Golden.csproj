<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <!-- Windows-only since the Objects area replays through the COM interop layer; still no Excel (CLAUDE.md R9). -->
    <TargetFramework>net10.0-windows</TargetFramework>
    <OutputType>Exe</OutputType>
    <UseMicrosoftTestingPlatformRunner>true</UseMicrosoftTestingPlatformRunner>
    <Description>Golden differential tests: cases recorded from real VBA, replayed against the runtime with no Excel present.</Description>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="xunit.v3" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\VbaNg.Compiler\VbaNg.Compiler.csproj" />
    <ProjectReference Include="..\..\src\VbaNg.Interop\VbaNg.Interop.csproj" />
    <ProjectReference Include="..\..\src\VbaNg.Runtime\VbaNg.Runtime.csproj" />
  </ItemGroup>

</Project>
